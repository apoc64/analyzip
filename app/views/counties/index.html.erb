<h3 class="center">Counties</h3>

<div class="row">
  <%= render partial: "partials/ranking_card", locals: { data: @presenter.card_1 } %>
  <%= render partial: "partials/ranking_card", locals: { data: @presenter.card_2 } %>
  <%= render partial: "partials/ranking_card", locals: { data: @presenter.card_3 } %>
</div>

<div id="map-info"></div>
<div id="map"></div>

<%= render partial: "partials/map" %>
