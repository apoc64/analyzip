<%= form_for @presenter.favorite do |f| %>
<%= f.label :comment %>
<%= f.text_field :comment %>
<%= f.hidden_field :zip_id, value: @presenter.code %>
<%= f.submit "Add this zip to my favorites" %>
<% end %>
