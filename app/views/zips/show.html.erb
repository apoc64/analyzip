<h3 class="center"><%= @zip.code %></h3>
<h6>Located in: <%= @zip.zip.state.abbreviation %></h6>
<h6>Number of Returns: <%= @zip.zip.n1 %></h6>
<h6>Percent Single: <%= @zip.zip.percent_single %></h6>
<h6>Percent Joint File: <%= @zip.zip.percent_joint %></h6>
<h6>Percent HOH: <%= @zip.zip.percent_hoh %></h6>
<h6>Percent Paid Prep: <%= @zip.zip.percent_paid_prep %></h6>
<h6>Average Number of Dependents: <%= @zip.zip.average_dependents %></h6>
<h6>Percent Elderly: <%= @zip.zip.percent_elderly %></h6>
<h6>Mean Adjusted Gross Income*: <%= @zip.zip.mean_income %></h6>

<div id="map"></div>

<script>
function initMap() {
  var options = {
    zoom: 13,
    center: {lat: <%= @zip.lat %>, lng: <%= @zip.lng %>}
  }
  var map = new google.maps.Map(document.querySelector('#map'), options);
}
</script>
<%= javascript_include_tag "https://maps.googleapis.com/maps/api/js?key=#{Rails.application.credentials.google[:api_key]}&callback=initMap", async: true, defer: true %>
